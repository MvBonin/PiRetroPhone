
import time
import os
import settings as s
class Alarm:
	self.isRunning = False
	def runAlarm(self):
		print("Alarm is run")
		
	def self._runAlarm(self):
		if(not self.isRunning):
			print("Thread is run")
			self.isRunning = True
